export default function App() {
  return (
    <main className="min-h-screen bg-surface text-content antialiased">
      <section className="mx-auto flex max-w-5xl flex-col gap-6 px-6 py-20 text-center">
        <p className="text-sm font-medium uppercase tracking-[0.2em] text-brand-500">Trolipay</p>
        <h1 className="text-4xl font-semibold text-balance sm:text-5xl">
          Premium waitlist experience is ready.
        </h1>
        <p className="mx-auto max-w-2xl text-base text-content-muted sm:text-lg">
          Vite React (JavaScript) + Tailwind CSS v4 are now scaffolded for frontend-only development.
        </p>
      </section>
    </main>
  );
}
